#ifndef A9N_HAL_ARCH_ARCH_TYPES_HPP
#define A9N_HAL_ARCH_ARCH_TYPES_HPP

#include <kernel/types.hpp>

// template of arch_types.hpp (for reference only)
// unlike other interfaces, it is used in critical areas.

namespace a9n::hal
{
    /*

    // memory
    inline constexpr a9n::word BYTE_BITS = 8;
    inline constexpr a9n::word PAGE_SIZE = 4096;
    inline constexpr a9n::word KERNEL_VIRTUAL_BASE = 0xFFFF800000000000;
    inline constexpr a9n::word USER_VIRTUAL_BASE   = 0x0000000000000000;

    inline constexpr a9n::word HARDWARE_CONTEXT_SIZE = 64;
    inline constexpr a9n::word FLOATING_CONTEXT_SIZE = 512;
    inline constexpr a9n::word VIRTUAL_CPU_CONTEXT_SIZE = 512;

    */
}

#endif
